---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}
  name: {{ .Release.Name }}
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8000
  selector:
    app: {{ .Release.Name }}
